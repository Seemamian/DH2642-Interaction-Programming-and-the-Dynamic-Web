import { Plugin } from 'vite';

/**
 * HtmlTemplate Plugin options.
 */
interface Options {
    /**
     * 是否是 MPA multi-page application
     */
    mpa: boolean;
}
declare type UserOptions = Partial<Options>;

declare function htmlTemplate(userOptions?: UserOptions): Plugin;

export default htmlTemplate;
export { UserOptions as HtmlTemplateOptions };
